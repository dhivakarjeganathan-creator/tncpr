# Database Configuration
DB_TYPE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hierarchy_db
DB_USER=postgres
DB_PASSWORD=test@1234

# Presto/Watsonx Data Configuration (if using Presto)
# Uncomment and configure if using Presto instead of PostgreSQL
# DB_TYPE=presto
# PRESTO_CATALOG=hive
# PRESTO_SCHEMA=your_schema

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=False